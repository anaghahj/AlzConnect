<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background-color: #eaf3f3; /* Pale blue */
            /* background-color: #e0f6ef; */ /* Pale green */
            background: linear-gradient(to bottom, #8e44ad, #3498db);
            background-repeat: no-repeat;
            background-size: cover;
        }

        .container {
            width: 500px; /* Increased width of the container */
            padding: 20px;
            border: 1px solid #ccc;
            background-color: #fff;
            opacity: 30;
            border-radius: 5px;
            opacity: 0.8;
            margin:0;
            justify-content: center;
            align-items: center;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-group label {
            display: block;
            font-family: monospace;
            font-weight: bolder;
            font-size: larger;
            margin-bottom: 5px;
        }

        .form-group input {
            width: 100%; /* Expanded the width of the input fields */
            height: 40px; /* Increased the height of the input fields */
            padding: 6px; /* Added padding to the input fields */
            margin-right: 10px; /* Added margin between input fields and container on the right side */
            border: 1px solid #ccc;
            border-radius: 4px;
            background-color: #E1AEFF;
            
        }

        .btn {
            background-color: #DD58D6; /* Blue button color */
            /* background-color: #32a852; */ /* Green button color */
            color: #fff;
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        .btn.signup-btn {
            background-color: #40128B; /* Green button color for signup */
        }
        .forgot-password-btn {
            background-color: #32a852; /* Green button color for forgot password */
            margin-left: 350px;/* Added margin between login and forgot password buttons */
        }
        
    </style>
</head>

<body>
    <div class="container" >
        <h1 style="font-family: monospace;text-align: center;">LOGIN</h1>
        <form action="/login" method="post">
            <div class="form-group">
                <label for="username">USERNAME</label>
                <input type="text" id="username" name="name" style="font-size: larger;" required>
            </div>
            <div class="form-group">
                <label for="password">PASSWORD</label>
                <input type="password" id="password" name="password" style="font-size: large;" required>
            </div>
            <button type="submit" class="btn">Login</button>
        </form>
        <form action="/forgot" method="get">
            <input type="submit" class="btn forgot-password-btn" value="Forgot Password">
        </form>
        <form action="/signup" method="get">
             <label style="font-family: monospace;font-size:small;font-weight: bold;"><br>Don't have an account ? Just Sign Up!</label>
        <input placeholder="Dont't have an account? Just sign Up" type="submit" class="btn signup-btn" value="Sign Up">
       </form> 
    </div>
</body>
</html>
